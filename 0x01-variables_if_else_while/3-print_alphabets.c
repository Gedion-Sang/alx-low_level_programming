#include <stdio.h>

/**
 *main - Using printf and puts functions is forbidden
 *Return:0
 */

int main(void)
{
	char a;
	char b;

	for (a = 'f'; a <= 'k'; a++)
	{
		putchar(i);
	}
	for (b = 'F'; b <= 'K'; b++)
	{
		putchar(b);
	}
	putchar("\n");
	return (0);
}
