#include "main.h"

/**
 * main - function that prints the alphabet, in lowercase
 * Return:0
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
	_putchar(i);
	}
	_putchar('\n');
	return (0);
}
